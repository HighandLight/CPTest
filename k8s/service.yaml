apiVersion: v1
kind: Service
metadata:
  name: hikari-experiment-service
spec:
  selector:
    app: hikari-experiment
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP